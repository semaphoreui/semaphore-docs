---
title: Reference
description: Exact values rather than explanations - every configuration option and every CLI command, generated from the Semaphore source.
---

# Reference

This section is for the moment you already know what you want and need the exact
name, type, or default. It explains nothing; the guides do that.

The pages here are **generated from the Semaphore source code** on every release, so
they describe the version you are running rather than the version someone wrote a
table for. For the same reason they are published in English only: a translated copy
would go stale the first time an option changed.

## In this section {#in-this-section}

| Page | What it covers |
|---|---|
| [Configuration options](/reference/configuration) | Every key of `config.json` with its environment variable, type, and default. |
| [CLI reference](/reference/cli) | Every `semaphore` command, subcommand, and flag. |

## Related pages {#related-pages}

These are written by hand because they explain rather than list:

- [Configuration](/admin-guide/configuration) — how options are supplied and which ones matter first.
- [CLI](/admin-guide/cli) — the administration tasks the commands are used for.
- [API](/admin-guide/api) — authenticating and driving Semaphore programmatically.
